# Configuración de Seguridad Frontend

# Usar sessionStorage en lugar de localStorage para tokens (más seguro)
REACT_APP_USE_SESSION_STORAGE=true

# Tiempo de expiración del token en milisegundos (2 horas = 7200000)
REACT_APP_TOKEN_EXPIRATION=7200000

# URL del backend API - RAILWAY PRODUCTION (ACTIVO)
VITE_API_URL=https://soporte-backend-production.up.railway.app

# URL del backend API - DESARROLLO LOCAL (BACKUP)
# VITE_API_URL=http://localhost:3001

# Configuración de logging (solo en desarrollo)
REACT_APP_ENABLE_DEBUG_LOGS=true
